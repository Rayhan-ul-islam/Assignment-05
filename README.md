Name of your project
= Dev Stack

A little description
= এটা একটি Dev Stack ওয়েব সাইট. বিভিন্ন ধরনের Technology নিয়ে েএখানে কাজ করা হয়। বিশেষ করে JavaScript

Technology that you use
= আমি এখানে Specally React নিয়ে কাজ করছি।

3 features about your project
=   1. এখানে NavBar এবং সুন্দর একটি Banner/Hero Section ব্যবহার করা হয়েছে যা খুবি আকর্ষনীয়
    2. বিভিন্ন ধরনের Technology নিয়ে কার্ড তৈরী করা হয়েছে যার সব গুলোই Functional
    3. শেষে একটি ব্যবহার Footer করা হয়েছে।

Also answer these React questions at the end of your Readme (write the answers in your own words, short and simple):

What is JSX, and why is it used in React?
= JSX-এর বাংলা সহজ অর্থ: JavaScript XML | React এর ভিতরে HTML-এর মতো কোড লেখার একটি syntax

What is the difference between props and state?
= Props: Parent component থেকে child component-এ data পাঠাতে ব্যবহার হয়।
= State: Component-এর নিজের data। State পরিবর্তন হলে component আবার render হয়।

What does the useState hook do, and where did you use it in this project?
=useState: Component-এর data store ও update করার জন্য ব্যবহার করা হয়

What does the useEffect hook do, and why did you need it to load the JSON data?
=data fetch করার জন্য useEffect ব্যবহার করা হয়

Why does every item in a .map() list need a unique key prop?
=.map() করার সময় প্রতিটি item-এর unique key দেওয়া দরকার হয় । এটা রিয়েক্টের নিয়ম । এতে প্রতিটা আইটেম রিয়েক্টের আলাদা করতে সুবিধা হয়।

What is conditional rendering? Show one place you used it (example: the empty stack message).
=Conditional Rendering User এর চাহিদা অনুযায়ি লিজিকার কাজ করে কে UI পরিবর্তন করে

How do you pass data from a parent component to a child component, and how does a child send something back to the parent?
=